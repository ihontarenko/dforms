validation:
  DEFAULT:
    messages:

      NULL_OBJECT:
        message: "Field cannot be null"
        pointer: name

      STRING_BLANK:
        message: "Field cannot be blank"
        pointer: name
        context:
          FORM_NAME:
            message: "Form name cannot be NULL"
            pointer: formName

      UNSUPPORTED_CHARACTERS:
        message: "Field contains unsupported characters"
        pointer: name
