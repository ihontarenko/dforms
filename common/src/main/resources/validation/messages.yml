validation:
  DEFAULT:
    messages:

      NULL_OBJECT:
        message: "field cannot be null"
        pointer: name

  FIELD_VALIDATION:
    messages:
      INCOMPATIBLE_USAGE_TYPE:
        message: "a VIRTUAL column must have an element type of NONE"
        pointer: usageType